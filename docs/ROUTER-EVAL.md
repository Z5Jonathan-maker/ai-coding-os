# Router Eval

`cc-router-eval` replays `fixtures/router/eval.json` against `cc-route`.

The fixture is intentionally small and private-workflow-shaped: image direction,
Kimi/browser UI work, Claude precision review, DeepSeek transforms, and Codex
repo execution. It prevents quiet regressions when classifier rules change.
