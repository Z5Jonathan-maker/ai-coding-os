---
name: feedback-corpus-as-brain-standing-directive
description: "Founder standing directive 2026-07-15: 'as the corpus grows your brain grows — really pull from the correct corpus to solve problems and get more intelligent.' Every strategy/quant problem-solving loop MUST start by consulting the mined corpus for mechanism-grounded hypotheses BEFORE reasoning from defaults or blind parameter grids. The corpus is an ACTIVE reasoning input, not a passive citation. This makes 'get smarter' real at the SYSTEM level (retrieval + generation + memory), not model-magic."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 35aa6961-2e1d-4bbc-93f4-b7df230489be
---

**Founder directive (2026-07-15, repeated + emphatic):** "Let all the research we've been doing actually have a part in your reasoning — you should be getting smarter. You have the top education/studies/theories/strategies on the planet and you're not using it as you build and test." + "As the corpus grows your brain grows. I want to see you really pulling from the correct corpus to solve problems and get more intelligent." + "We figured out ways to mine weeks of content in minutes using API."

**HARD BEHAVIORAL RULE:** on ANY strategy/quant problem (improve a near-winner, fix a weakness, design a test), the FIRST step is to consult the mined corpus for the mechanism-grounded levers for that problem class — NOT reason from model defaults, NOT run blind parameter grids (blind grids = the dumb version the founder called out). Every proposed lever must carry (a) a corpus SOURCE and (b) an economic MECHANISM (why prices behave so it helps); mechanism-less = data-mining, reject. Retrieval = `mempalace` MCP (semantic) + `rg` into `~/.claude/wiki/learnings/` (28+ mentor brains + aligrithm mega-brain + quant-methodology-canon, GROWING) + the repo forensics/decisions.md. PROOF IT WORKS: ORB5 + a corpus-grounded volatility-regime gate lifted PF 1.23→1.32 (MNQ) / 1.16→1.31 (MES) — a corpus idea, not a random knob.

**THE LOOP THAT MAKES "corpus grows → brain grows" REAL (system-level, verified pieces present 2026-07-15):**
fast API intake (weeks-in-minutes: session-authenticated API harvest + QKMS public-API crack) → `mega-brain-ingest` digests → `~/.claude/wiki/learnings/` (28 mentor brains, growing) → `engine/acquire/corpus_reader.py` (engine reads the corpus) → `engine/generator/corpus.py` + `synthesis.py` (corpus-derived economic PRIMITIVES → testable strategy candidates) → the kill-by-default gauntlet (honest fills / OOS / deflation at cumulative-K) → `engine/state/` K-ledger + candidates + graveyard + auto-memory (accumulate). More corpus → more retrievable mechanisms → better hypotheses → more tested → the validated set grows. Being codified as VALIDATION-GAUNTLET **phase-0 "corpus-informed hypothesis"** step (wf wg0ilcp1e). The discovery-engine generator IS corpus-fed (corpus.py + synthesis 33-primitive economically-grounded library).

**NONSTOP ENRICHMENT MANDATE (founder, 2026-07-15 evening):** "Make sure the corpus continues to get bigger, constantly enriching non stop." Enrichment is not episodic — a durable nightly intake job (corpus-enrichment-engine workflow wf_2fb9ebc9-3a8 wired `scripts/corpus_intake.py` + launchd in mym-autotrader) pulls tranches from every open vein (aligrithm remainder → 373 done, trader tapes via public APIs, Pine rules via pine-facade, academic papers on open gaps, MR-vein specs) → distills to mechanism-grounded dig-queue cards → generator consumes → gauntlet tests. A completeness critic lists uncovered gaps each run. If the dig-queue is ever thin or the intake log is stale, that IS a defect — restart the intake.

**HONEST MECHANISM (don't oversell):** I (the model) do NOT auto-learn between sessions. "Get smarter as the corpus grows" is true at the SYSTEM level — retrieval + generation + memory + the standing reasoning discipline above — carried forward by memory into every session. So the config that delivers the founder's ask = (1) this directive in memory (every session pulls corpus-first), (2) the corpus-fed generator, (3) the growing corpus via fast intake, (4) the phase-0 method step. Related: [[project-mym-qkms-northstar]] (intake north-star, public-API-first behind the gauntlet), [[project-mym-discovery-engine]] (the engine), [[project-mym-fill-fidelity-verdict]], [[feedback-backtest-measures-robot-not-trader]].