#!/usr/bin/env bash
# Quick sync: stage everything in ~/dotfiles, commit with given msg, push.
set -euo pipefail
cd "$HOME/dotfiles"
git add -A
if git diff --cached --quiet; then
  echo "nothing to commit"
  exit 0
fi
msg="${1:-chore: sync dotfiles}"
git commit -m "$msg"
git push
