---
name: project-mym-vault-os-clone
description: "MYM VAULT OS — the unified trading dashboard built by cloning Algory's HUD verbatim and re-skinning to Nebula; where it lives, how it runs, what's left."
metadata: 
  node_type: memory
  type: project
  originSessionId: 0bfa0844-774f-4912-bf3d-aa0416d3eb83
  modified: 2026-07-22T00:54:07.451Z
---

Started 2026-07-21 to kill the trading-dashboard sprawl (founder: "3 desktop icons, different themes, I hate it — clone Algory entirely by code, build on top, aggressively use Algory+Nebula exact code").

**What exists (all in `~/code/projects/mym-autotrader`):**
- `docs/os/` — Algory HUD frontend lifted verbatim (10 files: index.html=Command Deck, runs/configure/strategies=Vault/book=Portfolios/brain/insights/diagnostics.html + shell.js chrome + controls.js), re-skinned to Nebula slate-blue/gold/green + IBM Plex via `docs/os/_reskin.py` (idempotent color/font remap; brand wordmark ALGORY→MYM VAULT).
- `service/os_bridge.py` — Algory's HUD server RECONSTRUCTED as stdlib ThreadingHTTPServer (Algory's real backend shipped PyArmor-compiled = unrecoverable, so re-implemented to speak the identical route protocol). Serves docs/os/ + Algory endpoint vocabulary from `docs/data/*.json`. **READ-ONLY by construction**: no live-config import, no subprocess/writes; every command-plane endpoint (/launch /kill /autopurge…) returns {disabled:true}. ETag/304 on /telemetry. Port 8792 (`OS_BRIDGE_PORT`).
- Runs via `com.jonathan.mym-vault-os` launchd KeepAlive; single icon `~/Desktop/MYM VAULT.app` (chromeless Chrome --app on :8792).

**FULLY WIRED 2026-07-21:** `service/os_bridge.py` now implements every page's real data contract (reverse-engineered via 7-agent workflow wf_41898293-bb3 — full specs in that journal). All ~40 endpoints map edge-scoreboard/hq-state/strategy-intel/world-state/judge-verdicts/constellation → the exact JSON shapes each cloned page reads. Vault page verified fully live (23 real passports w/ DSR/Sharpe/PBO/verdict). Every Algory command-plane POST (/launch /deploy /vaultdelete …) hard-returns {ok:false,disabled}. Note: headless-Chrome screenshots race async fetches (pages show LOADING); they render fine in the real MYM VAULT.app Chrome.

**Backend crack reality:** Algory's backend (`deobfuscated/out/algory_hud_server.py`, `Algory_Engine_v129.py`) decompiles to 4-5 line docstring-only stubs — PyArmor-BCC compiled the bodies to native, UNRECOVERABLE. Only `deobfuscated/gene_registry.py` (4406 lines, shipped un-obfuscated) is real backend source. os_bridge.py faithfully reconstructs the documented `algory_hud_server` architecture (stdlib ThreadingHTTPServer, 127.0.0.1, POST-only mutations, /telemetry from a data JSON) — so cloning the "cracked backend" = my reconstruction, wired to mym's real engine instead of Algory's MT5-genetic one.

**iMessage + chart-image alert lane (NEW, lil.fish-inspired):** `service/setup_card.py` renders a Nebula-themed YM setup card PNG (quarter-figure ladder + bias + PDH/PDL + governor/floor footer) from bias-today.json via headless Chrome. `service/imessage_notify.py` sends text+image via osascript (account-selector + timeout wrapper; recipient = env MYM_IMESSAGE_TO, unset by default). `service/notify.py` = central `alert(title,body,image)` fanning to ntfy + iMessage. KEY FINDING: ntfy.sh free tier hard-429s ATTACHMENTS (text ok), which is exactly why lil.fish uses iMessage for chart pics — iMessage is the image channel. To go live: `export MYM_IMESSAGE_TO=+1...`.

**THE FULL EXPERIENCE = `docs/hq-vault/` on :8770 (mym-dashserve), URL `http://127.0.0.1:8770/hq-vault/`.** AREA61 VAULT Cockpit — 14 hash-routed surfaces (#cockpit #vitals #book #engine #nursery #insights #intel #results #lab #forward #genome #replay #constellation #roster), fully styled + populated. `~/Desktop/MYM VAULT.app` now opens THIS (repointed 2026-07-21 from the sparser Algory-clone :8792). Algory-clone :8792 is the secondary table view. hq-vault assets don't resolve under :8792 sub-path — keep it on :8770 root.

**ROOT-CAUSE FIX 2026-07-21 (why the dashboard looked empty/frozen): `hq/build_hq_state.py:286` crashed every run for ~10.6h** — `acct_id.startswith(a.get("prefix", "\0"))` hit an account with `prefix: null` (key exists → default unused → startswith(None) TypeError), freezing hq-state.json at 638 min old. Fixed to `a.get("prefix") or "\0"`. Rebuilds clean now; com.jonathan.mym-hq-state back to exit 0, 300s cadence. If the Cockpit looks stale again, check this job FIRST: `bash hq/refresh.sh`.

**LOOP ACTIVATION 2026-07-21 (why it felt "not turned on"): core loop (hunter PID + gauntlet) was always grinding ~hundreds-thousands/day (peak 2653), but THREE brain feeds were FROZEN 70-82h — `intel.json`, `judge-verdicts.json`, `world-state.json` — because nothing rewrote them.** Fix: folded their writers into `hq/refresh.sh` (world.render + export_strategy_intel.py + gen_vault_snapshots.py + brain_telemetry.py), which the `mym-hq-state` launchd job runs every 300s. Verified auto-firing (feeds < 5min old on their own). If any brain feed goes stale again → check `hq/refresh.sh` runs clean: `bash hq/refresh.sh`.

**BRAIN-IMPROVEMENT TELEMETRY (NEW): `service/brain_telemetry.py` → `docs/data/brain-telemetry.json` + durable daily `engine/state/brain_history.jsonl` (upsert one row/day, survives log rotation, tags model era).** Computes brain-index-over-time from candidates.jsonl (tested_at timeline), beliefs.jsonl (992 learning events), edge-scoreboard (winners), k-ledger, intel atoms. HONEST multi-axis `summary.dimensions`: learning/knowledge/throughput ~80-90 (capability HIGH) but **yield ~2.6** (survivor_rate 0.13%, 13 winners / 10k tested) — deliberately separate so it never fakes yield. This is the truthful instrument: brain compounds in capability; the frontier is YIELD (real edge is scarce), matching the honest verdict that mechanical edge is thin + the discretionary/human component is real. Two K3-built visual panels served on :8770/hq-vault, in DECK MAP nav: **REVIEWER (REV)** = `reviewer.html` (verdict ledger + why-won/lost + feeding-the-brain), **BRAIN INDEX (IQ)** = `braintel.html` (hero index + trend line + throughput + dimensions).

**FOUNDER THESIS (2026-07-21): "not a static brain" — K3 + Fable 5 + Opus 4.8 infused, compounding as models+data improve.** Agreed direction: route discretionary-edge elicitation THROUGH the multi-model brain (interview → encode as testable hypotheses → honest-fill test → learn), and watch whether model infusions lift the YIELD axis specifically (not just learning). Full mechanical autonomy is likely the wrong north star given the honest kills; AI-augmented-discretionary is where the edge lives and is most of what's built.

**Remaining:** wire newsvendor pitch/z-gate into Confidence Lab (needs per-strategy returns/n feed from forensics/); route discretionary elicitation through the brain; corpus re-feed to lift throughput toward the 64/cycle budget (currently ~9-11, corpus source dry).

**Full review + roadmap:** `~/code/projects/mym-autotrader/audits/2026-07-21-platform-architecture-review.md` (5-agent sweep + 7-agent verify). The money path (`service/app.py` :8000 → validator → CrossTrade → Tradeify) and `engine/` firewall are UNTOUCHED and stay that way. Related: [[project-mym-hq-command-center]], [[reference-nebula-harvest]], [[project-mym-institutional-brain]].
