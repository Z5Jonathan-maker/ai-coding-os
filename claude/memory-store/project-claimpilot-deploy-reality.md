---
name: project-claimpilot-deploy-reality
description: "ClaimPilot/Eden CRM deploy: ONE source = main; push to main auto-deploys backend (Render) + frontend (Vercel eden2). NEVER CLI-deploy from temp dirs."
metadata: 
  node_type: memory
  type: project
  originSessionId: 01f58f71-11e4-4a96-9873-14afda337feb
---

## DECONGESTION PASS — 2026-06-09 (repo hygiene, builds on the 06-06 flow below)

Repo cleaned in-session 2026-06-09 (audit: `docs/audits/2026-06-09-decongestion-audit.md`): untracked ~62MB of tracked-but-gitignored data (9 Xactimate price JSONs + code library — NO runtime readers, prices live in MongoDB `db.xactimate_prices`; 12 proprietary carrier playbook JSONs; .vercel/.emergent/.trae/.gitconfig leftovers; e2e-results; dead craco.config.js). **The price JSONs now exist ONLY locally in `backend/data/` — do not delete that dir; regenerate via `parse_plx.py --batch`.** 14 Eden-era root docs + memory/ specs + plans/ + thoughts/ archived to `docs/archive/`. Local branches → main + master only (snapshots preserved as tags `archive/*`). **GitHub default branch flipped master→main 2026-06-09.** ship.sh at repo root = push main + verify Render deployed HEAD (Render key in keychain `render-api-claude`). PENDING USER OK as of the session: pushing the 5 cleanup commits (`./ship.sh`) + deleting 4 merged remote branches (feat/email-intelligence-ui, feat/hardening-tests, feat/brain-currency-engine, backup/contacts-module-source-of-truth — all merged into main). Follow-ups flagged: frontend/public/icons = 35MB/42 PNGs (compress pass), ARCHITECTURE.md stale (Mar 21), optional git filter-repo history shrink (87MB pack).

## ✅ VERIFIED CANONICAL DEPLOY FLOW — 2026-06-06 (supersedes all "final/unified" claims below)

Everything below this block is HISTORY. The June-2 "DEPLOY UNIFIED (final) / autoDeploy ON / risk GONE" claim was **over-claimed and did not hold** — verified 2026-06-06: Render autoDeploy was OFF, and the frontend was being CLI-deployed from a gutted `/tmp/unified` scratch dir, not git. Now fixed and END-TO-END VERIFIED:

- **ONE source of truth: branch `main` of `github.com/Z5Jonathan-maker/ClaimPilot`.** Both services auto-deploy from it.
- **Backend** → Render `Eden` (`srv-d64ljiffte5s738aqdf0`), branch `main`, **autoDeploy = yes (turned ON 2026-06-06 via API — it had been OFF)**. Push to main → auto-deploys. Manual trigger: `POST https://api.render.com/v1/services/srv-d64ljiffte5s738aqdf0/deploys` with RENDER_API_KEY (keychain `claimpilot-scraper`).
- **Frontend** → Vercel `eden2` (`prj_QoKcIvnttBsPM9LK4JbpKE58czWN`, team `team_sgUnwrDyrMp8pPKOIZN0qUlz`), **git-linked to ClaimPilot, productionBranch=main, rootDir=frontend** (Vite, `npm run build` → `build/`). Push to main → auto git-deploy (verified via alias `eden2-git-main-*`). Custom domain **eden.careclaimsadjusting.com** auto-points to the latest prod git deploy.
- **TO DEPLOY: just `git push origin main`.** Do NOT `vercel --prod` from a local/temp dir — that scratch-dir habit (gutted `/tmp/unified`, stale cross-scope `.vercel` link) caused all the drift. Scratch dirs `/tmp/unified` `/tmp/consol` `/tmp/keeper-fe` DELETED 2026-06-06.
- **Local working copy: `~/code/projects/ClaimPilot` is now on `main`** (was master — the stale design line). Edit here, push, done. `master` left intact as history; never deploy it.
- Frontend is Vite (`craco.config.js` in frontend/ is leftover cruft — ignore). 179 src files = complete tree.
- Fixed this session (2026-06-06): evidence-packet empty-window (`noopener` on window.open), weather over-conservatism (QC down-adjusters now caveat instead of disqualify) — both on main, both live.

---

ClaimPilot/Eden CRM consolidation — the branch/deploy reality (non-obvious, took deep investigation 2026-06-01):

**COMMS + TRANSPARENCY 2026-06-02 (branch `feat/email-intelligence-ui`, NOT merged — external-facing, needs QA):** User's #1 goal = higher-level communication/transparency w/ clients + referral partners. Built 3: (1) internal Inbox/email-intelligence UI (src/components/inbox/* + emailApi.js, route /inbox+nav, consumes existing email-log/intelligence/actions/gmail-sync; SyncStatusBanner → Settings/Integrations since google_connected=FALSE). (2) Client transparency: routes/client_transparency.py `/api/client-transparency/claim/{id}/timeline` (stage/milestones/next-steps + SANITIZED carrier-comms from email_logs — strips body/notes/strategy/$figures; client_email access-scoped) + ClientClaimDetails/ClientStatusPanel timelines. (3) Referral portal: routes/rep_portal.py `/api/rep/my-referrals` (high-level status only, whitelist projection, NO financials) + ReferralPartnerPortal.jsx route /partner + notify_referral_partner hook. APPROVED SCOPE: clients see status+milestones+next-steps+carrier-comms-timeline (no $/strategy); partners see high-level status + milestone notifs. Boot 908, vite builds, 952 tests green. **OPEN/CAVEATS: (a) referral↔claim linkage = FREE-TEXT commissions.referral_source match (no referral_partner_id field) — CONFIRM how referrals are tracked or harden; (b) notify_referral_partner hook NOT wired to stage changes yet; (c) Gmail OAuth disconnected → reconnect Settings/Integrations (user action); (d) external portals need access-scoping tests + QA before merge to live customer site.** FOURTH branch off main awaiting review.

**HARDENING — TESTS + VITE 2026-06-02 (branch `feat/hardening-tests`, NOT merged/deployed):** CORRECTION to earlier "zero tests" claim — backend HAD 989 tests (76 files) but unrunnable (no test DB). Built harness: `dependencies.py` uses mongomock-motor when TESTING=1 (module-level `db` shared); conftest boots in-process app + seeds users/claims + auth-token fixtures; pytest.ini. Suite now **952 passed / 0 failed / 0 errors / 44 principled-skips / 2 xfail** (skips = Twilio/Stripe/AI-provider/SignNow/real-mongo-$text-aggregation). Removed 5 orphaned tests (pruned estimating). Added `tests/test_authz_regression.py` (10 tests) locking the IDOR fixes. Fixed 4 REAL prod bugs found by tests (harvest badge mutation 500, voice toggle NameError, harvest rewards None/bool, notifications is_read). FRONTEND CRA→Vite: react-scripts+craco removed (815 pkgs), Vite 6, 47 process.env.REACT_APP_*→import.meta.env codemod, index.html→root, index/App→.jsx, vitest+RTL. **Production npm audit 45 vulns(21 high)→0.** Build→build/ (Vercel-compatible), backend-ref baked, preview 200. CAVEATS: tests use mongomock (real-mongo features skipped, not in CI yet); Vite build verified but NOT app-run/click-through validated — needs QA before merging to live customer site. 4 prod bug fixes reach prod only on merge. THREE branches now await review/merge off main: feat/brain-currency-engine, feat/hardening-tests (+ main=live).

**BRAIN CURRENCY ENGINE — FOUNDATION BUILT 2026-06-02 (branch `feat/brain-currency-engine`, NOT merged/deployed, boot-verified 904 routes):** Three layers feeding a human review gate. (1) Foundation `services/brain_currency.py`: currency metadata (status/effective_date/last_verified/superseded_by/version/provenance, backward-compat missing⇒active), lifecycle (add/supersede/verify/approve/staleness-report), recency-aware retrieval (excludes superseded + "as of <date>"/staleness in Eve prompt), admin API `/api/knowledge-base/currency/*` + idempotent backfill. (2) notebookLLM ingestion `services/brain_ingestion.py` + `/api/knowledge-base/ingest/{text,url,preview}` (chunk→LLM-structure→pending_review, supersession-candidate detect; borrowed satvik314/opensource_notebooklm). (3) wikiLLM self-improve `services/brain_self_improve.py` + `/api/knowledge-base/learn/{from-claim/{id},correct,from-conversation,preview-learnings}` (extract_learnings/distill-from-resolved-claim PII-anonymized/capture-correction flags supersession candidate; borrowed laramohan/wikillm generate_facts, dropped MEMIT). All LLM via existing `_send_via_ai_gateway` (=Claude once bridge live), admin-gated, nothing auto-publishes. Spec: `backend/docs/brain-currency-engine.md`. REMAINING (tomorrow): vector/semantic retrieval (needs embedding provider — Anthropic has none; Voyage/OpenAI/local), scheduled FL-legal ingestion automation, statute-version tracking, wire self-improve triggers + review UI (frontend), mega-brain-ingest adapter, MERGE TO MAIN after review+smoke-test. Caveat: ingestion/self-improve quality needs Claude wired (Eve bridge, pending token).

**BRAIN = CURRENCY ENGINE (core requirement, user-stated 2026-06-02):** FL public-adjusting law/statutes/tactics change constantly — a STATIC brain gives dangerous outdated answers (cites amended statutes / dead tactics) = liability. The Brain MUST self-evolve + stay current. Design implications for the Brain-intelligence project (sequenced after Eve-on-Claude): every knowledge section needs provenance + effective-date + last-verified + staleness flag; SCHEDULED ingestion of latest FL statutes/DFS rules/case law/carrier bulletins (use their cron/Routines/schedule-task); SUPERSESSION/conflict-resolution (retire/mark-superseded old content when new contradicts — don't just append, or Eve cites both); statute VERSION tracking w/ effective dates; retrieval must prefer recent/verified + Eve surfaces "current as of <date>" + flags uncertainty on stale/unverified knowledge; human-review/approval gate before auto-ingested changes become authoritative (legal accuracy); wikiLLM self-improve loop (resolved claims + corrections feed back, dated/versioned). Patterns: notebookLLM clones (satvik314/opensource_notebooklm, OpenDCAI/Open-NotebookLM) for vector-RAG ingestion; Karpathy wikiLLM (laramohan/wikillm) for self-maintaining KB. Hard parts = supersession + statute-version tracking + review gate, NOT just vector search.

**EVE → CLAUDE SUBSCRIPTION BRIDGE 2026-06-02 (staged, blocked on user's iMac token):** User wants Eve (CRM assistant) powered by their Claude Max $200 sub, NOT OpenAI, NOT metered API, NOT cc-router (cc-router is their separate CODING multi-model system — leave it alone). Verified compliant: `claude setup-token` gives a 1-yr `CLAUDE_CODE_OAUTH_TOKEN` officially for "CI/scripts"; June 15 2026 Agent SDK + `claude -p` on subs draw from a monthly Agent-SDK credit. Internal CRM (2-3 own-office users, own claims) = allowed; only marketed/distributed products are barred. BUILT: private repo `eve-claude-bridge` (Dockerfile: node + @anthropic-ai/claude-code; `server.cjs` = `/v1/messages` → `claude -p --model claude-opus-4-8 --output-format json` using CLAUDE_CODE_OAUTH_TOKEN). Eve already calls `/v1/messages`; made `routes/ai_claim_workspace.py` base-URL configurable via `ANTHROPIC_BASE_URL` (default unchanged, inert). RESUME when at iMac: (1) `claude setup-token` → copy token; (2) create Render web service from eve-claude-bridge (Docker), set env CLAUDE_CODE_OAUTH_TOKEN=<token> + CLAUDE_MODEL=claude-opus-4-8; (3) on eden-gsot set ANTHROPIC_BASE_URL=https://<bridge>.onrender.com + ANTHROPIC_MODEL=claude-opus-4-8 + provider order anthropic; (4) verify Eve answers on Claude. CAVEAT: `claude -p` headless-in-Docker untested — may need config-dir/TTY tuning; fallback metered key if flaky.

**RUTHLESS AUDIT + REMEDIATION 2026-06-02 (done, live):** Report at `docs/audits/2026-06-02-ruthless-audit.md`. Fixed+deployed to `main` (auto-deploys both services, all boot/build-verified, live verified health 200 / login OK / 175 claims / Brain 200 / frontend 200): removed dead `litellm` pin (cleared the 1 critical CVE); closed IDOR trio — uploads.py `/my-files`+`/content` (claim-access gate), `/gmail/send-email` (require_role admin/manager), SignNow status/download (scoped via contracts.signnow_document_id), Gamma create/update/append (require_role interim, no id→claim map); 7 medium items (knowledge_base auth, payments active-user, httpx timeouts, to_list caps, re.escape, email audit-log); pruned ~12.7K LOC abandoned ScopePilot/estimating (List A 6 routes+15 services + List B damage_intake/pricing/floorplan+6 services) + 6 dead heavy deps (rembg,onnxruntime,PyMatting,numba,scikit-image,opencv); deleted 6 dead frontend components; untracked build/. Routes 799→890. KEPT (shared with live supplements/scales): supplement_engine, estimate_schema, scope_inference, carrier_estimate_parser, parser cluster, reportlab/lxml. STILL OPEN (user actions): rotate Google Maps key `AIzaSy…U17R8` + Gemini key `AIzaSy…F13Lk` (both in git history); confirm Stripe policy exemption (stripe active in backend vs global ban); ESLint react-app preset + CI=true blocked (react-hooks plugin conflict + unfixed warnings — needs dedicated lint-cleanup pass); console.log strip (needs babel dep).

**DEPLOY UNIFIED 2026-06-02 (final):** ClaimPilot now has ONE canonical branch **`main`** with clean structure: `/backend` (Brain, 799 routes) + `/frontend` (keeper CRA/craco). BOTH services auto-deploy from `main`: Render `eden-gsot` (srv-d64ljiffte5s738aqdf0, rootDir=backend, autoDeploy ON) + Vercel `eden2` (prj_QoKcIvnttBsPM9LK4JbpKE58czWN, rootDir=frontend, productionBranch=main, git-connected to ClaimPilot, env REACT_APP_BACKEND_URL=https://eden-gsot.onrender.com). The keeper frontend's exact-live CSS (AA-contrast delta) was folded in; live cutover verified byte-faithful (bundle main.062e9153→main.adb445f4, darker-orange AA button intact). Branches now: `main` (canonical), `master` (old design line, non-default — deletable later), `backup/contacts-module-source-of-truth` (brain safety). Deleted unified/keeper-crm + consolidate (==main). NOTE: disabled Vercel deployment protection on eden2 (preview URLs now public; app is login-gated so harmless). Vercel API rejects slash branch names for productionBranch — that's why the canonical branch is `main` not a slash-name. The "deployed-from-a-lost-device" risk is GONE.

**CLEANUP DONE 2026-06-02 (final clean state):** Goal was consolidation/organization, not a feature build. Outcome — KEEPER CRM = live eden.careclaimsadjusting.com: frontend = ClaimPilot `master:frontend/frontend` (CRA/craco, react-scripts 5.0.1, 136 components, builds clean); backend = eden-gsot ← ClaimPilot `consolidate/design-onto-brain-2026-06-01` (full Brain, 799 routes). The live frontend was historically deployed via CLI from a local dirty copy (commit 9446b552, a few commits ahead of master — "AA contrast/CSS/webfont"); exact-live source recovered from source maps + saved at ~/code/projects/careclaims-crm-recovered (also still on Vercel). ClaimPilot branches cut 28→3: kept master, consolidate, backup/contacts-module-source-of-truth; deleted 21 dependabot + 2 codex + feat/contacts-module + feature/claimpilot-rebrand (520 commits archived first to tag `archive/claimpilot-rebrand-20260602`). Repos deleted: `Eden` (migrated into ClaimPilot, docs in docs/eden-legacy/) + `careclaims-next` (redundant w/ marketing). KEPT repos: ClaimPilot, careclaimsadjusting-website (Eleventy marketing site, separate), careclaims-instagram (unique IG automation — keep/extract TBD). STILL OPEN: (1) reconcile master(frontend)+consolidate(backend) into ONE branch so Vercel+Render auto-deploy cleanly (currently divergent + the keeper frontend deploys dirty, autoDeploy off on both); (2) optionally fold the 9446b552 dirty delta into master; (3) careclaims-instagram decision.

**CORRECTION 2026-06-02 (important): the brain branch's FRONTEND is the ABANDONED old ClaimPilot app — NOT the keeper.** The keeper / pastor-demo app is the live **eden.careclaimsadjusting.com** = the CRA "design-shell" frontend (Care Claims ink/paper design, ~50 components, bundle `main.062e9153.js`, from the master design line). The "design-onto-brain" frontend graft was therefore aimed at the WRONG codebase — that Phase 0 work was reverted and the throwaway `claimpilot-design-preview` Vercel project deleted. **The backend consolidation is KEPT** (user's call): eden-gsot runs the enriched brain backend (799 routes, commit `4e395ac`), contract-compatible with the keeper frontend (which only calls ~12 core routes; all 175 claims intact). CONFIRMED BY USER: keeper = live eden.careclaimsadjusting.com AS-IS (what they showed pastor). It is a LEAN claims CRM that only calls 5 API namespaces — /api/auth, /api/claims, /api/notifications, /api/payments, /api/users. It does NOT surface the Brain (no /api/eve, /api/university, /api/knowledge, /api/brain calls). The "Brain" is the expensive scraped Florida-PA expert corpus (carriers, code-sections, scope-templates, mitigation, Xactimate) that powers Eve + University + knowledge — it is PRESERVED and live on eden-gsot (University 31 routes, knowledge-base 20, /api/brain/* 8, Eve routes) + the prod Mongo data. The user explicitly wants the Brain kept (cost a lot to scrape). What they abandoned: the WHOLE ClaimPilot/ScopePilot autonomous-estimating PROJECT + its dark-theme marketing landing page (that was the brain-branch frontend — reverted/deleted). So: surfacing Eve/University/knowledge in the keeper app later = NEW UI work on the keeper design-shell, powered by the already-live Brain backend. Rollback of the backend to `407d998c` remains available if the live app shows issues.

**UPDATE 2026-06-02: consolidated brain backend SHIPPED to prod.** `eden-gsot.onrender.com` now runs branch `consolidate/design-onto-brain-2026-06-01` (canonical brain backend flattened to repo-root `backend/`, boot chain fixed, +numpy/opencv). 799 routes live (was 513); gmail-sync/email-log/email-actions/email-intelligence now live. Staged on eden-2 first, then promoted; eden-2 re-suspended. One regression hotfixed: `GET /api/claims/` default `limit=50` → 500 (April was unbounded), commit `4e395ac`. Rollback target: `dep-d7nv8r0k1i2s739vcfgg` (407d998c). **Still open:** frontend is still the CRA design-shell (no UI for new brain routes — Phase 2 design graft pending, route to KIMI); master not yet fast-forwarded to the deployed branch.

Original investigation (still valid context):

- **Live prod (`eden-gsot.onrender.com`) ran commit `407d998c` from Apr 27, 2026** (now superseded — see UPDATE above). It was *orphaned* — not reachable from `origin/master`; survived only as the running Render deploy. It is an ancestor of `feat/contacts-module-2026-05-30` (the "brain" branch = prod + 313 commits).
- **`master` is a DIVERGENT design/brand/SEO line**, forked at ancient ancestor `1ebe083`. Its backend is a near-strict subset of the brain (master-only backend: `notion.py` + `notion_service.py`). Master's real value = frontend design layer (ink/paper tokens, Care Claims rebrand, DESIGN.md, SEO/GEO). The Sherlock premise "master = polished prod shell, port brain onto it" was INVERTED.
- **Render service `Eden` (`srv-d64ljiffte5s738aqdf0`) has `autoDeploy: no`.** Pushing master does NOT deploy. A second service `Eden-2` (`eden-2.onrender.com`, `srv-d64uudp4tr6s738rvb2g`) has autoDeploy ON for master — verify it isn't a live surface before treating master pushes as safe. rootDir=`backend`, start `uvicorn server:app`.
- Brain branch backend is nested under `frontend/frontend/backend/` — does NOT satisfy rootDir=backend, so it can't deploy as-is without flattening.
- Render API token: `~/.render/cli.yaml` → `api.key`; CLI re-auth via `render login` (device-code, browser).

**Why:** the prior session assumed master auto-deploys and mistook month-old April routes for a fresh partial deploy; the whole "Wave 1 partial deploy mystery" was that nothing ever deployed.

**How to apply:** never deploy master to the Eden service expecting consolidation — it would wipe 215 commits of live features. Recommended direction is design-onto-brain. Full analysis: `~/code/projects/ClaimPilot/docs/eden-legacy/3-LINE-CONSOLIDATION-MAP.md`.
