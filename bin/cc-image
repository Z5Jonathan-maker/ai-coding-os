#!/usr/bin/env bash
set -euo pipefail

router_root="${CC_ROUTER_ROOT:-${ROUTER_ROOT:-$HOME/code/projects/cc-router}}"
CLIENT="$router_root/lib/openai-image-client.cjs"

[ -f "$CLIENT" ] || {
  echo "cc-image: missing client at $CLIENT" >&2
  exit 1
}

exec node "$CLIENT" "$@"
