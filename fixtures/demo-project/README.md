# AI Cockpit Demo Project

Small public fixture used by `cc-demo-fixture`. It gives evaluators a harmless
repo for route, repo-map, diff, and verification proof without depending on any
private project.

```sh
npm test
```
