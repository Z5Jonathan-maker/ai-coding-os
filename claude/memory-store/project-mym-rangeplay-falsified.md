---
name: project-mym-rangeplay-falsified
description: "MYM RangePlay overnight range-fade engine FALSIFIED 2026-06-29 — live 0-for-7 + fill-realism audit proved PF 1.31 was an overlapping-lot + phantom-limit-fill artifact; pause it, drop from the book, and the RangePlay-based S-TIER/corrected-economics playbooks' overnight line is void"
metadata:
  node_type: memory
  type: project
  originSessionId: 2371a7ee-9ee2-4e08-89fc-b6ddec492160
---

2026-06-29: The "RangePlay" overnight range-fade engine (one of the 6-strategy Sim101 forward-test book; backtest `ping_pong_v2/v3/v4.py`, detector `service/rangeplay_live.py`, bot `ninjascript/RangePlay_v1.cs`, doc `vault/04-playbooks/overnight-rangeplay.md`) is **FALSIFIED**. Jonathan flagged it: live it produced straight losses twice (4 back-to-back 2026-06-29 + a straight-loss run the prior week), contradicting its PF 1.28–1.31 / 7-of-7-green backtest.

**Live evidence:** 0-for-7 on Sim101, EVERY trade a stop, max-favorable-excursion ≈0–5pt before the 18pt stop (entries immediately offside — not good fades that lost). It faded the SAME QF both directions in one night (06-29 LONG 52375 then SHORT 52376, both stopped) = price grinding THROUGH a QF pivot, no real range. Plus a double-fill bug 06-26 02:13 (two identical qty-1 RangeS same instant).

**Root cause (re-audit, `ping_pong_v4.py`, agent a4f0b0d):** the edge is a FILL ARTIFACT, NOT a live-detector bug (detector is byte-faithful, 100% entry parity). Headline PF 1.31 = (i) ~**64% of gross from unrealizable OVERLAPPING lots** (2-bar cooldown stacks up to 18 simultaneous positions; true serial = PF 0.98 at $4.5 RT, near breakeven), and (ii) **PHANTOM FILLS** — the entry condition books a fill at the exact QF whenever a bar's wick comes within tol=12pt (`l<=qf+tol`), even when price NEVER traded at the QF. **79.4% of backtested fills are phantom** (un-fillable by a real resting limit); the only honestly-fillable 21% (price punches THROUGH the wall) average **−1.0pt / PF 0.50** = pure adverse selection = exactly the live losing subset. Honest "bar-must-contain-entry-price" limit-fill model → **PF 0.41–0.50**. Costs alone ($4.5 RT vs the $1.5 one-sided used) take the headline to 0.87.

**Actions:** pause RangePlay (don't promote past Sim101); drop it from the protected monitor book (else the VPS monitor re-deploys it — it heals to exactly-1-per-class). Treat the +$6–7k/yr/micro line as VOID. **Cascade:** the RangePlay-based playbooks — S-TIER three-firm stack (~$94k/mo, `s-tier-three-firm-stack.md`) and the live-brain-corrected-economics LEAD (`live-brain-corrected-economics.md`, EMPIRE-OS current lead) — have their OVERNIGHT economics built on this falsified engine and need re-work; the intraday fades (§12/Sniper/Precision) + ORB are separate and stand. Salvage (optional, low priority): a limit-at-the-wall fade is dead; the phantom "winners" were reversals that never reached the wall, so capturing them needs a CONFIRMED-REJECTION market/stop entry (candle closes back inside off the wall), rebuilt + re-validated from scratch with serial execution + bar-contains-price fills + $4.5 RT — nothing in the current param space clears PF 1.0 under those rules.

**The whole overnight QF-mean-reversion FAMILY is dead (2026-06-29, "is there a hidden winner?" check):** RangePlay = ping_pong_v4 (Darvas box); its ancestors are `backtest/ping_pong.py` (original directional) → v2 (established-range) → v3 (indicator gates) → v4. The thing RangePlay *replaced*, ScalpRotation_v1 (7-yr discretionary QF scalp bot), was itself already falsified ([[project-mym-overnight-scalp-falsified]]). Re-audited the ORIGINAL directional ping_pong under honest+causal fills: it does NOT share v4's phantom-fill flaw (0% phantom — its `open<z and high>=z` is a true cross-the-level entry, survives serial+1m+slip at PF 2.82) BUT it has a DIFFERENT fatal flaw — a same-bar LOOKAHEAD in the EMA bias gate (direction from the bar's close while filling intrabar); causal fix drops PF 1.73→1.33, win 76%→61%, 0 neg years→4-of-6 negative, and ~100% of net P&L is 2022 alone (a one-off rangebound year). So none of the lineage (original / ScalpRotation / RangePlay) is a survivor. The ONLY validated QF edges are the NY-OPEN lanes (§12/Sniper/Precision) + the discretionary AREA61 method — NOT any mechanized overnight/quiet-hour fade. Don't revert to ping_pong; the overnight income sleeve is $0.

**Bigger lesson (reinforces [[feedback-backtest-measures-robot-not-trader]] + the 1-min-fill artifact that already voided the scalp sleeve):** a resting-limit mean-reversion strategy backtested on 1-min bars that books fills at price-touched-within-tol is structurally optimistic — it captures un-fillable phantom reversals and misses the adverse selection of real limit fills. Any limit-fill strategy must be validated with a "bar's range must contain the entry price" fill model + realistic RT cost before trusting it. Worth gut-checking the §12/Sniper QF-ladder fills aren't flattered the same way (they enter market-ish on momentum, so likely fine — confirm, don't assume). Relates to [[project-mym-mtf-gate-golden-standard]], [[project-mym-qf-falsified-orb-validated]], [[project-us30-live-signals]].

**FINAL VERDICT (2026-06-29, strict-box + honest-fill test of his EXACT spec):** his real QF-scalp (limit AT the QF, tight stop just beyond, target=opposite wall, re-load) is DEAD under honest fills even with STRICT causal box selection (both walls held >=2/3/4 before entry, QF-anchored, overnight low-vol, ATR<median → PF 0.46-0.59, all losers, == falsified loose RangePlay) AND even with a PERFECT box-hold oracle (PF 0.74, still negative). Killer = ~82% of honest limit fills are price PUNCHING THROUGH the QF (win 21%); the reject-and-revert scalp is only ~18% (win 43%). Adverse selection is INTRINSIC to a resting limit at the QF, NOT a box-quality problem — so even perfect detection can't make a full-auto bot work, and box-selection isn't even the edge; the edge is finer = the real-time read of which specific TAP rejects vs punches. → NO full-auto limit-at-QF bot, ever. Build a SCANNER CO-PILOT: bot detects/draws/alerts the genuine QF-anchored rectangle + manages the bracket AFTER the human triggers; the HUMAN owns the entry trigger. One untested max-automation idea = rejection-confirmed entry (tap→close-back-inside→enter) but that deviates from his at-the-QF spec. Strongly matches [[project-mym-qf-falsified-orb-validated]] (his live edge = irreducible discretionary selection → scanner-to-phone, not a full-auto bot).

**BREAKTHROUGH UPDATE (2026-06-29): the CONFIRMATION STOP-ENTRY is a real survivor.** [⛔ RETRACTED same day — see CORRECTION at end: the PF 1.90 below was a SURVIVORSHIP-ACCOUNTING ARTIFACT; the exploratory loop silently DROPPED the ~51% of fills that flatted on a box-break/EOD (all ≈−18pt). Honest one-trade-per-fill accounting = PF 0.54, all years red. The confirmation-stop bot is DEAD. Do NOT trust any number in this paragraph.] Research (2 arxiv papers + Ernie Chan) confirmed the naive resting limit is structurally adverse-selected (documented 4:1–12:1 on CME futures; round-number STOP-CASCADES are the punch-through mechanism) and the pro cure = a confirmation/stop entry. Tested it: replace the resting limit AT the QF with a CONFIRMATION STOP-ENTRY (tag the QF → a bar closes BACK OFF the level = rejection shown → enter on a STOP 1 tick beyond the confirmation bar's extreme; only fills if price continues, NEVER on a clean punch-through). FIRST variant in the whole audit to SURVIVE honest causal fills: PF 1.90 (stop=18pt), win 43%→72%, dodges ~35% of punch-fills, IS PF 2.03 ≈ OOS PF 1.97, ALL 6 years green (NOT 2022-concentrated), ~126 trades/yr, survives costs to PF ~1.4. KEY: direction is NOT the edge (counter-trend = PF 1.96 ≈ with-trend 1.90) — the CURE IS THE ENTRY MECHANIC (confirmation stop), not box/bias/selection; "with-trend only" was a red herring for this variant. CAVEATS (do NOT oversize): (1) MARGINAL ~$500/yr/micro, needs scale; (2) it's a 1-MIN-BAR PROXY for the real tick/L2 order-flow confirmation — edge is real on bars but live fill-achievability is UNPROVEN → Sim101/demo FORWARD-TEST the fills before trusting size (same fill-assumption class that burned RangePlay); (3) needs 12–18pt stop (looser than his tightest 5–8) + single-entry (reload → PF 0.49). Winning config: detect_box N=2-3 (both walls held, look=30 on 5m, range 35-130, flat_tol=10, overnight 18:00-05:00 ET, ATR(14,5m)<median) + tag-QF→close-back-off(≤3 bars)→stop 1tk beyond confirmation bar + stop 12-18pt + target opposite wall + single entry. This is the confirmed-rejection entry flagged early as the only salvage — the founder's push + the research got us to actually test it. NEXT: build confirmation-stop bot (Pine+NinjaScript) for Sim101 forward-test + scanner co-pilot (his live tick-read likely beats the bar proxy). The overnight sleeve is NOT dead — it's a real-but-marginal bar-accessible edge pending forward-validation.

**CORRECTION / FINAL-FINAL (2026-06-29 — retracts the BREAKTHROUGH above): the confirmation-stop PF 1.90 does NOT exist; honest PF 0.54, the QF scalp is NOT mechanizable.** When the result was formalized into a clean canonical backtest (`backtest/confirmation_scalp.py`, uncommitted, docstring "FALSIFIED. DO NOT PORT."), it exposed the 1.90 as a survivorship-accounting bug: the exploratory loop counted only trades that resolved at the stop (302) or the opposite-wall target (721) and silently DROPPED the 1,062 of 2,085 fills that flatted on a box-break / EOD (modal outcome, avg −18.6pt). Honest one-trade-per-fill accounting: N=2,085, win 40.7%, **PF 0.54, −$1,281/yr/micro, IS 0.57 ≈ OOS 0.52, all 6 years RED.** The confirmation entry DOES dodge ~37% of punch-fills (works as designed), but the modal fill reaches neither stop nor the far opposite-wall target before the consolidation breaks — and nearer fixed-R targets don't rescue it (all 0.42-0.58). So EVERY mechanical version of the QF scalp is breakeven-or-dead: naive limit ~0.5, with-trend-only ~1.0 (spec-violating wide stop), confirmation-stop 0.54. **The QF rectangle scalp is NOT mechanizable — full stop.** His edge is irreducibly his real-time discretionary read of which consolidation holds + which tap to take (matches [[project-mym-qf-falsified-orb-validated]]). DELIVERABLE = SCANNER CO-PILOT ONLY: `detect_box` is verified/sound → flag the genuine established QF-anchored rectangle + the confirmed rejection to his chart/phone (Pine indicator + optional ntfy); he owns the trigger. DO NOT build the bot. PROCESS NOTE: the "lock the canonical backtest before porting to live code" discipline caught my own inflated 1.90 BEFORE any Pine/NinjaScript port or Sim101 deploy — the parity-anchor gate did its job. Reinforces [[feedback-backtest-measures-robot-not-trader]] + [[feedback-verify-anthropic-facts]]-style verify-before-claiming, applied to my OWN numbers.
