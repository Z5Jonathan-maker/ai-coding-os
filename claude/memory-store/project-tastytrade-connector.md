---
name: project-tastytrade-connector
description: "mym-autotrader tastytrade live-execution connector — built, sandbox-default, blocked on refresh_token + account_number pending Jonathan's account/futures validation (KYC doc proof)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9657b9db-35c4-472b-acac-64c579e41efb
---

Jonathan chose **tastytrade** as the live broker for the mym-autotrader bots (2026-06-15). Build state:

- **connectors/tastytrade.py** — full BaseConnector impl: OAuth2 refresh-token auth (grant_type=refresh_token, 14min token refresh), SANDBOX-default (api.cert.tastyworks.com; live=api.tastyworks.com), bracket/OTOCO orders for micro futures (MYM/MNQ). Same interface as sim_local/tradovate so the validated strategy runs unchanged (no drift).
- **scripts/tastytrade_smoke.py** — one-command sandbox test (connect -> balances -> place+cancel test bracket). Sandbox-only guard. Run: `python -m scripts.tastytrade_smoke`.
- **config.yaml** (gitignored) has the `tastytrade:` block with client_id + client_secret already filled (DO NOT re-handle/echo — they're local). environment=sandbox.

**EMPIRICALLY CONFIRMED against tastytrade's API (2026-06-15):** client_id+client_secret ALONE cannot authenticate — POST /oauth/token grant_type=client_credentials returns 400 unsupported_grant_type on both cert AND prod. Auth REQUIRES the refresh_token (grant_type=refresh_token; dummy token returned 400 "Invalid JWT" = endpoint wired, awaiting a real token). tastytrade v7 removed session auth; OAuth-only.

**BLOCKED ON:** `refresh_token` + `account_number` — Jonathan must first validate his account / get futures enabled (KYC doc proof in progress 2026-06-15), then generate the OAuth refresh token (My Profile -> API -> create grant -> authorize). Order-placement JSON (complex OTOCO + futures symbology /MYMM6) is built to spec but to be confirmed in the sandbox smoke test before live.

**ALL 3 STRATEGIES WIRED + PARITY-LOCKED + TRIPLE-AUDITED (2026-06-15):** live detectors built for all three, each parity-checked to its canonical backtest: S1 breakout-retest (service/breakout_live.py) 99.7% (0.3% = intentional 1-signal/bar cap + no last-bar entry, documented); S2 ping-pong (service/pingpong_live.py) 100% — config chosen via backtest/pingpong_sweep.py 18-config sweep, LOCKED to edge25/mid/with-bias (76% win, $189 DD; sweep PROVED counter-trading loses every way -> validates Jonathan's no-counter-trade rule); S3 coil (service/coil_live.py) 100% vs run_range_qf(buffer=30,wmax=0.5,cooldown=2). All 3 wired into scripts/run_bot.py routed through validator->execution->SimLocalConnector; parity scripts: backtest/parity_check{,_coil,_pingpong}.py. Triple-audited (3 agents): fixed warmup-loader parity bug (warm 5m detectors over 8d lead-in before counting signals), NaN guards, cooldown guard, config live-mode guard rejecting sim overlay values (stop_max>250/max_trades>10), sim-only assert. Re-verified post-fix: S1 99.7%/S2 100%/S3 100%, 130 tests green, clean demo (30 sessions: breakout 97, pingpong 4, coil 4 sim orders). Strategy-2 numbers UPDATED: ping-pong canon = edge25/mid/with-bias (NOT the old structure_ladder wide-stop version).

**(historical) breakout-retest engine wired first:** Pieces: service/breakout_live.py (streaming BOS+HL+retest detector, parity-locked to backtest/breakout_retest.py), service/execution.py (SHARED execute_validated_signal used by BOTH webhook + bot = no live/backtest drift; webhook.py refactored to call it, 130 tests green), scripts/run_bot.py (replay/live runner: 1m RTH databento bars -> detector -> WebhookPayload -> validator.validate (real rails) -> shared execution -> SimLocalConnector). app.py factory + config.py validation extended for tastytrade. Demo: 29 sim orders placed, all through the validator.

PARITY VERIFIED (backtest/parity_check.py): live detector vs backtest over 6yr = 99.7% identical trades (4660/4673 match by date/dir/QF; 0.3% = edge-of-window i+1 entry-timing diff, negligible). So the validated numbers (37% win, +3.3R, every-year-green) carry to the live bot by construction.

Triple-audit (3 code-reviewer agents) caught + FIXED: (1) parity — runner fed 5m not 1m + no RTH filter (WINDOW=90 meant 7.5h not 90min) -> fixed to 1m RTH; detector age off-by-one -> switched to brk_i index gate matching backtest. (2) connector — close_position falsely returned True when not actually flat/stale-token -> returns False unless it truly flattened + _ensure_token; OAuth error body log scrubbed (no secret leak); _resolve_symbol fails loud instead of sending bad /MYM. (3) safety — added tastytrade live-creds config gate; overlay stop floor 5->15; sim-only assert in run_bot.
**LIVE-READINESS LAYER BUILT (2026-06-15, during the ~3-day approval wait):**
- Connector hardened: `_req()` wrapper = single 401 re-auth retry on every call (fixes the stale-token false-success risk); `_resolve()` = front-month auto-resolution via GET /instruments/futures (config-pin still wins); `subscribe_fills` is now a REAL account-streamer (wss://streamer[.cert].tastyworks.com, connect+heartbeat+auto-reconnect background task, cancelled on disconnect). Imports verified.
- **scripts/run_bot_live.py** = the production real-time loop (run_bot.py is replay-only). Drives the same 5 parity-locked detectors -> validator -> shared execution -> connector + fill streamer on a 1m clock; 5m engines aggregate from 1m; per-instrument reset (YM/NQ breakout per RTH day, BTC per cal day, coil/pingpong continuous). ONE live-feed swap = BarFeed.poll() (stub reads databento tail; point at tastytrade DXLink candles for prod). Verified on sim: 184 orders placed, fills streaming, 130 tests green.
- **docs/GO-LIVE-RUNBOOK.md** = one-page checklist (approval -> config -> sandbox smoke -> 2wk fill test -> compound) + safety/kill switch + parity-verify commands.
- STILL needs sandbox confirmation once creds arrive: exact OTOCO order JSON + the streamer wire format (built to spec, untested without a live account).

**Next when token arrives:** drop refresh_token + account_number + contracts.MYM in config.yaml -> run smoke test (sandbox) -> confirm OTOCO fills + wire subscribe_fills/401-retry -> paper-test breakout-retest -> live only on explicit approval (environment sandbox->live, rotate chat-exposed keys first). Strategy proven (live textbook breakout-retest 2026-06-15, see [[project-qf-ladder-backtest-findings]]).

**LIVE AUTH VERIFIED 2026-06-16 (account approved).** Account = **5WI85303** (live, prod). tastytrade hands you only client_id + client_secret + account#; the **refresh_token is generated via my.tastytrade.com -> Manage -> OAuth Apps -> "Create Grant"** (requires 2FA; shown once; it's a `rt+jwt`, scope "read trade openid", aud=client_id, never expires). Stored in gitignored config.yaml (refresh_token came in via 2-message chunked paste — ROTATE it, it's in transcript). Live read-only check PASSED: OAuth refresh->access works, account reachable, position flat. **TWO findings:** (1) **account is UNFUNDED — every balance $0** (derivative-buying-power 0), so NO live trade can execute until Jonathan funds it; (2) front-month auto-resolve picked /MYMM6 expiring in 2 days -> **added a roll buffer** (skip contracts within `roll_buffer_days`=8 of expiry) so it now picks the liquid **/MYMU6** (Sept); health_check now reports actual 0.0 vs None. config.yaml stays mode:sim/environment:sandbox; live was used via in-memory override only for the read-only check.

**NO TASTYTRADE API PAPER ACCOUNT (verified 2026-06-16, research-scout).** QuantConnect docs verbatim "The tastytrade API doesn't support paper trading." The cert/sandbox IS API-accessible but quotes are **always 15-min delayed** (+ futures-fill support unconfirmed) -> useless for fill-quality testing. So realistic futures testing = our **local sim** (behavior) or a **funded live account** (real fills). There is no paper middle-ground. Cert sandbox still worth a free signup ONLY to capture the real fill-JSON shape to finalize `connectors/tastytrade._normalize_fill` (the live-gate; raw streamer payload still UNVERIFIED — it logs the first raw fill + drops unclassifiable ones fail-safe).

**DEMO (local sim) FULLY WIRED 2026-06-16.** scripts/run_bot.py now drives the full lifecycle: signals -> validator -> execution (IN_TRADE on placement) -> sim entry fill -> `_resolve_exit` walks the 1m tape for stop/target/EOD -> `conn.simulate_exit` -> FillHandler records -> rails. Per-session-day `new_day()` reset re-arms daily rails. 30-session demo: **rails FIRE from real fills** (28 STAND_DOWN on max-consec-loss, 83 trades closed, +$3,832 sim/1ct). Gotcha fixed: pandas 2.x microsecond index -> `.asi8` mismatched `Timestamp.value` (ns); use `DatetimeIndex.searchsorted`. **DESIGN FINDING:** the rails are ACCOUNT-WIDE (1 trade at a time, 2/day, 2-consec-loss) so all 5 strategies across 3 instruments serialize -> BTC (24/7) eats the daily budget first and the RTH strategies got 0 fills in the demo. Decide before scaling: per-instrument/per-strategy budgets vs account-wide cap.
