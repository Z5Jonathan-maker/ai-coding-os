#!/usr/bin/env bash
set -euo pipefail

CLIENT="$HOME/code/projects/cc-router/lib/openai-image-client.cjs"

[ -f "$CLIENT" ] || {
  echo "cc-image: missing client at $CLIENT" >&2
  exit 1
}

exec node "$CLIENT" "$@"
