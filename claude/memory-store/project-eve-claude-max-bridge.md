---
name: project-eve-claude-max-bridge
description: How Eve (Eden CRM) uses the Claude Max subscription — token is set; this is the remaining wiring step
metadata: 
  node_type: memory
  type: project
  originSessionId: 01f58f71-11e4-4a96-9873-14afda337feb
---

Eve (the Eden/ClaimPilot CRM assistant) runs on the user's **Claude Max** subscription, not a paid API key (internal tool, 1-2 users — sanctioned for personal/internal use). See [[reference-fl-pa-law-verified-2026-06]] for the brain it draws on.

**Token (DONE, 2026-06-02):** A 1-year `claude setup-token` OAuth token (`sk-ant-oat01-`, scope `user:inference`) is stored as env var **`CLAUDE_CODE_OAUTH_TOKEN`** on the LIVE Render backend `srv-d64ljiffte5s738aqdf0` = service 'Eden' = eden-gsot.onrender.com (autoDeploy=yes from main). Also in the user's macOS Keychain ("Claude Code-credentials"). Regenerate anytime via `claude setup-token` if it's ever lost — the flow can be driven headlessly with a PTY (the user just approves the OAuth URL on their phone and pastes the `code#state` back). Token is single-view at creation; extract carefully — the CLI's box-formatting jams "Store" onto the end with no separator (trim it).

**VERIFIED call path:** the OAuth token authenticates directly against `https://api.anthropic.com/v1/messages` with header `Authorization: Bearer <token>` + `anthropic-version: 2023-06-01`.

**OPUS/SONNET ON THE MAX SUB (verified 2026-06-02 — corrects the old "opus is rate-limited, use haiku" belief):** the Max OAuth token serves the FULL lineup (incl. `claude-opus-4-8`) but ONLY when BOTH are present: (1) header `anthropic-beta: oauth-2025-04-20`, and (2) the FIRST `system` block is the Claude Code identity string exactly `"You are Claude Code, Anthropic's official CLI for Claude."`. Without both, opus/sonnet return immediate `429 rate_limit_error` and only haiku is served (that's why the naive Bearer-only path looked haiku-locked). Send `system` as a 2-block array: [CC-identity, real-prompt] — the second block (Eve's instructions) still governs persona/behavior (verified: Eve keeps voice + cites correct FL statutes). Also: `claude-opus-4-8` REJECTS the `temperature` param (400 "deprecated for this model") — omit it. **Eve now runs on `claude-opus-4-8`** (env `ANTHROPIC_MODEL=claude-opus-4-8` on Eden; `services/claude_max.py` sends the beta header + identity). **No fallback** (per user): backoff-retries the same model on 429/529 then RAISES — a successful `/api/ai/chat` therefore proves opus served it. Burst-tested 3 sequential msgs live: all 200, 2-4s, no 429. The user was right to push back when I claimed opus couldn't run — see [[feedback-verify-anthropic-facts]].

**REMAINING WIRING:** `backend/routes/ai_claim_workspace.py` `call_anthropic()` currently posts with `x-api-key`. To use the Max sub, switch it to read `CLAUDE_CODE_OAUTH_TOKEN` and send `Authorization: Bearer` instead (keep x-api-key as fallback). Then merge `feat/email-intelligence-ui` + deploy (Render autoDeploy is OFF — manual deploy needed). Until then the token just sits in env, unused.


**Minting a test token (verified 2026-06-02):** the JWT secret env var on Eden is named **`JWT_SECRET_KEY`** (NOT `JWT_SECRET`), alg `HS256`, var `JWT_ALGORITHM`. Token payload must use a REAL user `id` (e.g. `admin@eden.com`, an active admin) — `get_current_active_user` / `require_role` do a DB lookup, so a fake `sub` passes signature but 401s "User not found". Pull MONGO_URL/DB_NAME/JWT_SECRET_KEY from Render env-vars API, find an admin in `users`, sign with pyjwt. Render env-vars API returns a flat list; key is `e['envVar']['key']` or `e['key']`.

**CRITICAL — TWO RENDER SERVICES (drift trap, hit 2026-06-02):** `srv-d64ljiffte5s738aqdf0` = 'Eden' = **eden-gsot.onrender.com = THE LIVE BACKEND**. **As of 2026-06-02 Render autoDeploy is OFF — deploys are GATED through GitHub Actions CI** (`.github/workflows/ci.yml`): a push to `main` runs backend (lint + `python -m pytest`, 956 tests via mongomock) + frontend (vitest + vite build); ONLY if both pass does the `deploy` job POST to the Render deploys API (secrets `RENDER_API_KEY` + `RENDER_BACKEND_SERVICE_ID` in the GH repo). So a red CI = no prod deploy. To force a deploy, fix CI or POST to the Render API manually. Vercel frontend still auto-deploys on push (not yet gated). `srv-d64uudp4tr6s738rvb2g` = 'Eden-2' = eden-2.onrender.com = a DUPLICATE/staging with NO real traffic. Both share the same Atlas cluster0.h3qhd0n + db eden_claims (brain is shared). ALWAYS set env vars + verify deploys on the LIVE 'Eden' service. The Claude token was mistakenly set on Eden-2 for hours -> live Eve showed 'No AI provider configured' while everything looked deployed. Different JWT_SECRET per service, so mint test tokens with the LIVE service's secret. To verify Eve end-to-end: mint a token with Eden's JWT_SECRET, POST /api/ai/chat at eden-gsot.onrender.com.
