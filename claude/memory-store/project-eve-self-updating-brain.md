---
name: project-eve-self-updating-brain
description: How the Eden/ClaimPilot CRM brain stays current — scraper + Claude approval gate + weekly automation. Read before touching brain ingestion/scraping.
metadata: 
  node_type: memory
  type: project
  originSessionId: 01f58f71-11e4-4a96-9873-14afda337feb
---

The Eden CRM (eden.careclaimsadjusting.com; LIVE Render backend `eden-gsot.onrender.com` = srv-d64ljiffte5s738aqdf0 (NOT srv-d64uudp4tr6s738rvb2g — that's the Eden-2 duplicate); deploys are now CI-gated (autoDeploy OFF, GHA gate); Vercel `eden2` auto-deploys on push to main; repo `Z5Jonathan-maker/ClaimPilot`) has a **self-updating, self-vetting knowledge brain** that powers Eve. Built 2026-06-02. See [[project-eve-claude-max-bridge]] and [[reference-fl-pa-law-verified-2026-06]].

**Pipeline (every piece live in prod):** authoritative FL sources → ≥2025 freshness gate → Claude-Max structuring → **Claude approval gate** → `eve_knowledge_base` (status active = Eve uses it).

- **Sources** (`backend/data/scrape_sources.json`): 7 article/news (Property Insurance Coverage Law blog, Merlin, Tutwiler, Williams Law, Johnson Strategies, Insurance Journal-SE, NAPIA) + FL-PA YouTube channels (Merlin, The Claim Squad, Your Claim Hero, Claim Concepts, Alconero; Statewide/Ademar/Your-Claim-Guy added but no recent uploads).
- **Scraper** (`services/source_scraper.py` + `services/youtube_scraper.py`): RSS discovery (stdlib), dedup-on-success (`scraped_articles`), pacing, per-source caps. YouTube = channel-RSS discovery + **yt-dlp transcripts** (`youtube-transcript-api` is blocked; yt-dlp works). **YouTube transcript fetch only works from a residential IP** — datacenter/Render is blocked → YouTube scraping runs from the office Mac.
- **Freshness floor** (`brain_ingestion.ingest_document`, env `BRAIN_MIN_PUBLISH_DATE` default 2025-01-01): rejects pre-2025 sources; undated tagged `freshness=undated`.
- **Approval gate** (`services/brain_auto_review.py`): Claude Max vets each pending chunk vs verified current FL law. approve→active; clear trash (off-topic/promo/wrong/stale-as-current)→`rejected` (ARCHIVED, never deleted, recoverable); unverifiable legal claim (e.g. 2026 bills, case holdings)→stays pending tagged `review_verdict=needs_human` for the user. Conservative: never approve uncertain, never lose plausibly-valuable. Skips already-gated (`reviewed_by=claude_gate`).
- **Re-verification** (`services/brain_reverify.py`): quarterly live-statute tripwire + content-drift scan.

**Automation:** durable clone `~/code/ClaimPilot-scraper` (Python 3.11 venv) + `run_scrape.sh` (creds from macOS Keychain service `claimpilot-scraper`) + launchd `com.careclaims.scraper` **weekly Mon 8am** (residential IP). The scrape script runs scrape → auto-review in one pass (`SCRAPE_AUTO_REVIEW=1`).

**Gmail -> CRM new-claim scanner (added 2026-06-03):** `POST /api/intake/scan-gmail` (admin/manager; `services/claim_intake_scanner.py`) reads the caller's recent inbox via the stored Google OAuth token, opus-classifies each email (new_claim / existing_claim / not_a_claim) + extracts fields, dedups vs existing claims (carrier-claim#/policy#/client+carrier), and routes HYBRID: confidence ≥0.90 + min fields -> auto-create claim (`source='gmail_scan'`); else -> stage an intake submission (status 'new') for review in the existing /api/intake UI. Idempotent via `claim_scan_seen`. Tests: `tests/test_claim_intake_scanner.py` (6, mocked Gmail+LLM). Daily launchd `com.careclaims.claim-scan` **23:00 every day** (end of work day; Mac is on 24/7) runs `~/code/ClaimPilot-scraper/run_claim_scan.sh` (mints a token for the Gmail-connected user, POSTs the scan); alerts to `~/.claude/state/claim-scan-alerts.log` on 409/error. **Runs AS the Gmail-connected user (jonathan@careclaimsadjusting.com / 8f2db695), not admin@eden.com** — Gmail is connected per-user.

**Claim scan — existing_claim recovery + dedup (fixed 2026-06-03):** the scanner originally lumped `existing_claim` with `not_a_claim` and DROPPED both — so real active claims never entered in the CRM (mid-negotiation carrier threads) were silently skipped (53 such). Fixed: `existing_claim` now verifies against the CRM (`_is_duplicate`) — in CRM→skip; not in CRM + has identifiers (claim#/policy/name+carrier)→queue to intake_submissions for review (`queued_missing` counter); no identifiers→skip. Added pending-submission dedup (`_submission_already_pending`, dash/space-normalized) so one claim thread = one review item, not one-per-email. Fixed latent bug: `_stage_submission` now sets unique `submission_token` (unique index → null collision when staging 2+/run). Caps raised: lookback_days le 14→90, max_messages le 100→150 (but scan is SYNCHRONOUS + Opus-per-email ~6s, so true months-back times out; ~40-50 emails/run is the safe ceiling — a background job is the real fix, not yet built). To run manually off-network-DNS issues: the Atlas SRV lookup fails on the office router (192.168.254.254); point pymongo at public DNS via `dns.resolver.default_resolver.nameservers=["8.8.8.8","1.1.1.1"]` before importing MongoClient. **90-day catch-up (2026-06-04): 175 emails attached across 21 existing claims + 41 missing-claim submissions queued.** The scanner now ALSO attaches correspondence to existing CRM claims (`_attach_email_to_claim` → communications, deduped by gmail_message_id) so files stay current; nightly 2-day sync scan keeps them updated. **CRITICAL OPS LESSON: the `background=true` job mode is NOT durable on Render — the instance restarts (on any redeploy OR idle/memory spin-down) and silently kills the in-process asyncio task, leaving the job stuck "running" with 0 progress (happened twice 2026-06-04). For big backfills use SYNCHRONOUS chunked passes instead** (`background` omitted; step max_messages 90→150→300→450→600, each request completes & survives restarts; passes >~600 msgs time out the HTTP request at ~590s — server still processes server-side before client gives up). The nightly launchd job uses sync small (lookback=2,max=40) so it's reliable. **DURABLE FIX BUILT (2026-06-04): page-cursor mode** — `POST /scan-gmail?single_page=true&page_token=<tok>&page_size=50` processes ONE Gmail page and returns `next_page_token`; a driver threads the token to walk a months-long window in short restart-safe requests (each page ~seconds). This exhausted the full 90-day window (24 pages, `next_page_token` null at end) where background + big-sync both failed. Final catch-up state: **245 emails attached across 28 existing claims; 37 clean missing-claim submissions queued.** Also fixed: `_find_existing_claim` now matches claim/policy numbers leading-zero/dash-tolerant via `_id_regex` (a stored 015068 matches an email's 15068 — was mis-queuing existing-claim emails as "missing"). Triage helper pattern (reject in-CRM-by-normalized + automated-sender/subject + merge dupes) lives in the run history; re-run on the queue after big scans. Earlier same-day run:

**First real recovery + 90-day background backfill (2026-06-03) surfaced 19 unique active claims missing from CRM** (incl. both Darren Lewis claims CFL26611616 + CFL26611629, Clifton Kennedy, Alicia Muniz, Robert Fetterolf, Michael Fisher/USAA, Kasi Johnson, etc.), all queued to /api/intake review. Async backfill: `POST /api/intake/scan-gmail?background=true` → returns `{job_id}` (record in `email_scan_jobs`), poll `GET /api/intake/scan-gmail/jobs/{id}`; runs past HTTP timeout (90 msgs ≈ 4 min). Dedup now covers BOTH existing_claim and low-confidence new_claim queue paths via `_submission_already_pending` (carrier#/policy/name+carrier, dash+leading-zero normalized) so one claim = one review item. Manual cleanup of name-only near-dupes (same name, no claim#) still occasionally needed.

**Gmail OAuth CONNECTED + working (2026-06-03)** for jonathan@careclaimsadjusting.com (user 8f2db695). Getting there required fixing THREE stacked root causes — if Google ever disconnects, check all three: (1) prod `ENCRYPTION_KEY` was missing (see below); (2) the redirect URI `https://eden-gsot.onrender.com/api/oauth/google/callback` was not registered in the Google Cloud Console OAuth client `1004279075072-e9cto85i97ndimo6vnbu56a8chhokjpq` (user added it under "Authorized redirect URIs"); (3) the prod `GOOGLE_CLIENT_SECRET` was WRONG (Google returned `invalid_client`) — corrected to the real secret (`GOCSPX-…KFji`) from the same Console client page. Diagnostics that don't need a login: probe `https://accounts.google.com/o/oauth2/v2/auth?client_id=…&redirect_uri=…` and grep for `redirect_uri_mismatch` (tests redirect-URI registration); POST a dummy code to `https://oauth2.googleapis.com/token` and read the error — `invalid_client`=secret wrong, `invalid_grant`=secret good. Live dry-run verified: scans inbox, classifies existing/not-a-claim correctly.

**CRITICAL OPS GOTCHA — Eden autoDeploy is OFF (CI gate), so Render ENV-VAR changes do NOT auto-apply.** Setting an env var via the Render API saves it to config but the RUNNING instance keeps the old value until a deploy restarts it. This bit us hard 2026-06-03: the corrected GOOGLE_CLIENT_SECRET was saved but the live server kept 401'ing with the old secret for ~20 min until a manual deploy. After ANY env-var change on Eden, **trigger a manual deploy**: `POST https://api.render.com/v1/services/srv-d64ljiffte5s738aqdf0/deploys` (clearCache do_not_clear), then wait for live. (Code changes go through the CI gate; env changes need this manual kick.)

**CRITICAL — prod `ENCRYPTION_KEY` (found missing + fixed 2026-06-03):** `services/encryption_service.py` (Fernet) encrypts all OAuth tokens at rest (Google/SignNow/Encircle/Monday in `oauth_tokens`) and REQUIRES env `ENCRYPTION_KEY`. Prod (Eden srv-d64ljiff) had NONE set, so every stored token was undecryptable AND new connects would 500 at encrypt time. Generated a fresh Fernet key, set it on Render, and **backed it up in Keychain `claimpilot-scraper / EDEN_ENCRYPTION_KEY`** (root cause was the key never being persisted). CONSEQUENCE: the old tokens were encrypted with a lost key, so **all OAuth integrations must be RECONNECTED in Settings** (one-time) before they work — including Google, which the claim scanner needs (until then the scan returns a clean 409 "reconnect in Settings"). NEVER drop this env var again; restore from Keychain if it goes missing.

**Accuracy watchdog (added 2026-06-03):** launchd `com.careclaims.eve-accuracy` **weekly Mon 10am** (2h after scrape) runs `~/code/ClaimPilot-scraper/run_eval.sh` → mints an admin token (Keychain `EDEN_JWT_SECRET` = live prod JWT secret) → runs the canonical `backend/scripts/eve_accuracy_eval.py` fetched read-only from origin/main → 10 golden FL-law Q&As scored against live Eve. On any score <10/10 it appends to `~/.claude/state/eve-accuracy-alerts.log` + fires a macOS notification. Static sibling guard: `backend/tests/test_eve_legal_accuracy.py` (CI, every push). Both verified 2026-06-03 (CI 11/11, live 10/10). Run manually: `bash ~/code/ClaimPilot-scraper/run_eval.sh`.

**To run a scrape manually:** `~/code/ClaimPilot-scraper/run_scrape.sh` (or `backend/scripts/scrape_sources.py` with env). Knobs: `SCRAPE_LIMIT_PER_SOURCE`, `SCRAPE_PACING_SECONDS`, `SCRAPE_SOURCE_ID`, `SCRAPE_DRY_RUN=1`.

**Known caveats:** (1) brain_currency had a `(database or db)` Motor truth-test bug — fixed to `is not None`; watch for that pattern elsewhere. (2) `content_hash` unique index — `add_section` now computes sha256(title+content); null content_hash collides. (3) Eve runs on **claude-opus-4-8** as of 2026-06-03 — the Max OAuth token serves opus ONLY with `anthropic-beta: oauth-2025-04-20` + Claude Code identity in system block 0 (see [[project-eve-claude-max-bridge]]); claude_max now sends both, has 429 backoff, and NO model fallback (raises on sustained failure). The scraper itself still uses haiku (`ANTHROPIC_MODEL=claude-haiku-4-5` in run_scrape.sh) for cheap bulk review. STILL TODO (not started, behavior-changes — get user go): claim stage-progression past 'inspection', dual-regime deadlines, estimating $0 pricing (deferred ~2mo by user).
