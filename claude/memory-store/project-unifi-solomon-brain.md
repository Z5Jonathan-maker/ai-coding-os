---
name: project-unifi-solomon-brain
description: "UniFi's Solomon assistant — the expert \"brain\" architecture + moat strategy"
metadata: 
  node_type: memory
  type: project
  originSessionId: 30d30640-e9d6-47cc-9733-84ca2af078f8
---

Solomon (UniFi's in-app advisor) is the **moat** — strategy in [docs/SOLOMON.md](file:///Users/leonardofibonacci/code/projects/stewardship-app/docs/SOLOMON.md). Moat = 3 compounding layers: **conviction** (Word-of-Faith POV competitors won't build) × **personalization** (grounded in the user's real numbers) × **curation** (proprietary authored dossiers). Flywheel + church-network distribution. The "council" = expert brains Solomon embodies: CFO, **Give-first/Generosity** ⭐, Debt-freedom, Credit (CROA-careful, education-only), Investment (not licensed advice), **Tax/giving strategist** ⭐ (appreciated-asset/DAF giving = killer faith feature), Insurance, Estate/legacy, Kingdom-business, Behavioral, Real-estate. Knowledge × data: each specialist needs its feed (Plaid/Spinwheel/RentCast) — so the blocked integrations are now moat-critical.

**SHIPPED 2026-06-18 (RAG-lite, no vector infra):** `lib/brain/` — **59 authored dossiers across 16 domains** (cfo/giving/debt/tax/credit/invest/protect/legacy/behavior/save/budget/retire/buy/loans/health/biz) — comprehensive; ~9.7k-token cached block, nearing the point where domain-routing/embeddings pays off in `dossiers.ts` + a **dated currency layer** `reference-2026.ts` (time-sensitive 2026 facts: 401k $24,500, IRA $7,500, HSA $4,400/$8,750, std-deduction MFJ $32,200, brackets, + OBBBA 2026 charitable changes — new non-itemizer deduction $1k/$2k, 0.5% AGI floor, 35% cap). `buildBrainContext()` assembles wisdom + dated reference into a stable, prompt-cacheable block (~5.4k tokens), wired into `app/api/assistant/route.ts` as a 3rd cached system block (persona → brain → live snapshot). NOT pgvector yet (corpus fits cached context; `buildBrainContext()` is the seam to embeddings when it outgrows). Ideas attributed (Hagin/Copeland/Morris/Ramsey/Ron Blue/Alcorn/Bogleheads), text authored.

**Guardrails baked into persona + dossiers:** state current figures ONLY from the dated reference (name the year, never guess); investing = education, route specific securities to a fiduciary/CKA; credit = education + free FCRA self-dispute, NOT credit-repair (CROA); not licensed advice on big/irreversible moves.

**Competitive map** in docs/SOLOMON.md (research-scout 2026-06-18): faith competitors (FaithFi, EveryDollar) have ZERO AI = open lane; OpenAI acqui-hired+killed Hiro (faith = moat vs generic AI); ~$99/yr always-on-CFO slot empty (Range $2,950+); contrarian = solve behavior/accountability before comprehensiveness.

**To go live:** set `ANTHROPIC_API_KEY` server-side (Vercel) — else route 503s → keyword-mock fallback. Grounded in mock-data until Plaid+DB land (`buildFinancialContext()` swaps, route unchanged).

**Brain v2 SHIPPED dormant (2026-06-20)** — retrieval subsystem ported from the DoseCraft brain (`dosecraft-companion` "Wiki LLM + Notebook LLM" hybrid: Neon pgvector + FTS, contextual chunks, full-doc tier) with mid-2026 SOTA upgrades (research-scout verified; cited in [docs/SOLOMON-BRAIN-V2.md]). Built on UniFi's existing Neon+Drizzle+pg stack: `lib/brain/{embed,rerank,domain-index,store,retrieve}.ts` + `schema.sql` + `scripts/embed-brain.mjs` + `npm run brain:embed` + `brain-corpus/`. Engine = domain-routed hybrid (pgvector+FTS) → RRF fuse → **Voyage rerank-2.5** → **CRAG relevance gate** → full-doc notebook tier; embeddings = **voyage-4-large** (Anthropic's current official rec; A/B vs voyage-finance-2 on the real corpus; Gemini-001 leads independent MTEB, Voyage benchmarks self-published). **NOT full-agentic** (research: wrong call for curated/single-hop financial Q&A); citation-span groundedness is the planned add. `buildBrainContextAsync(query)` is the seam: retrieves when VOYAGE_API_KEY+BRAIN_DATABASE_URL+populated table exist, else falls back to the prompt-cached dossier block (route awaits it; today unchanged since route 503s before the call w/o ANTHROPIC_API_KEY). **KEY SEQUENCING: retrieval is premature until the corpus is BIG** — Solomon=59 dossiers (~10k tokens, fits in context) vs DoseCraft=246,799 chunks. GROW THE CORPUS FIRST (ingest financial mentors/books/IRS pubs into `brain-corpus/` as markdown w/ `domain:` frontmatter), then it activates. Activate: `psql $BRAIN_DATABASE_URL -f lib/brain/schema.sql` → set keys → fill brain-corpus/ → `npm run brain:embed`.

**Per-household MEMORY SHIPPED dormant (2026-06-20)** — the personalization moat: Solomon learns each household across sessions. `solomon_memories` table (Drizzle, household-scoped: kind/content/salience/sourceHash) + `lib/brain/memory.ts` (`recallMemories` + `buildMemoryContext` system block + `extractAndStoreMemories` — claude-haiku-4-5 extracts durable facts post-turn, deduped). Route now has a 4th context block (persona → brain → **memory** → snapshot) + `next/server` `after()` fires extraction post-response. Dormant-safe: no DATABASE_URL → recall empty + extraction no-op; empty memory block omitted (no empty system text). Activation needs a drizzle migration for `solomon_memories` (`npm run db:generate && db:migrate`). The full expert = persona × brain (general wisdom) × memory (their story) × snapshot (their numbers).

**CORPUS build progress (2026-06-20)** — `brain-corpus/` now **523 docs / 4,714 chunks / 8.7MB** (from 35 chunks at session start). Owner gave legal clearance (counsel on file) to ingest copyrighted mentor content fully. Layers: 16-domain conviction + prosperity dossier (authored/attributed) · 58 WEB Scripture passages · 4 PD wealth classics · 6 primary-gov docs · 13 mentor teaching docs · **474 mentor video transcripts** pulled on the user's RESIDENTIAL IP via `scripts/ingest-transcripts.sh` (yt-dlp; needs residential IP — datacenter gets blocked). Channels: Bogleheads/Ron Blue Institute/Compass/Crown/Ramsey Show + targeted Copeland/Hagin/Alcorn money-teaching searches + a retire lane. Transcript script cleans inline (rolling-caption dedupe + filler strip + reflow to ~75-word paragraphs; still lowercase/no-punct — embeddings handle it). `_DOMAIN_INDEX` routing key = the 16 domains. Re-run/raise caps anytime for more depth. Old 22-doc seed line superseded. (1) Conviction layer COMPLETE — all 16 domains authored/attributed (copyright-safe, WoF + Christian-canon + public-domain WEB Scripture). (2) Primary-source layer — 6 cited gov/Bogleheads docs (IRS 590-A/969/526, SSA, CFPB/myFICO, Bogleheads three-fund, studentaid.gov) via research agent; volatile $ deferred to reference-2026, durable statutory thresholds kept. Source map + copyright boundary in `brain-corpus/CORPUS-PLAN.md` ("gold of 2026": primary/gov + Ron Blue/Alcorn/Burkett/Ramsey-mechanics + Hagin/Copeland/Kenyon/Roberts + Bogleheads/Housel/FIRE + Scripture). NEXT SCALE LAYER = transcript-ingest of that mentor roster via mega-brain-ingest (dozens→thousands of chunks, the DoseCraft pattern).

**Next frontier:** (1) wire the data feeds — Spinwheel→credit, Plaid-Investments→invest, RentCast→real-estate (knowledge×data; integrations now moat-critical); (2) embeddings/pgvector when the corpus outgrows context; (3) behavior-first product features (the contrarian). Refresh `reference-2026.ts` yearly. Theology stays Word-of-Faith, no cessationist sources ([[project-unifi-monarch-clone]]). DoseCraft brain ([[project-dosecraft-brain-architecture]]) = architecture blueprint, not content.
