#!/usr/bin/env bash
# Convenience wrapper: latest GitHub verification code from Mail.app.
# Pass-through to mail-code with sender pinned to noreply@github.com.
exec "$(dirname "$0")/mail-code" "noreply@github.com" "$@"
